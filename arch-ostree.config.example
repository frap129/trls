# Configure settings for arch-ostree
# The values in this example config are the defaults. In your
# arch-ostree.config, you only need to define the values you wish to
# change.
# 'scriptdir' refers to the path of the arch-ostree checkout

builder_stages=()
builder_tag="archlinux-ostree-builder"
ostree_repo="/sysroot/ostree/repo"
sysroot_dir="/mnt"
deploy_env_dir="$scriptdir/deployment"
src_dir="$scriptdir/setup"
podman_build_cache=false
pacman_cache=
aur_cache=
build_stages=()
extra_contexts=()
rootfs_tag="archlinux-ostree-rootfs"
